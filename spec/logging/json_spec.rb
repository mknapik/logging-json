require "spec_helper"

describe Logging::Json do
  it "has a version number" do
    expect(Logging::Json::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
